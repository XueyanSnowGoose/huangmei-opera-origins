# ============================================================
#  白名单式 .gitignore:除下列文件外,其余一概不纳入仓库
#  (原书扫描件 / 全本 OCR / 中间数据 / 脚本 / 缓存 等全部排除)
# ============================================================

# 1) 先忽略一切
*

# 2) 再仅放行需要发布的文件
!.gitignore
!index.html
!README.md
!LICENSE

# 说明:
#  - 白名单方式比逐条列出要排除的文件更安全:任何新增的
#    原书扫描件、OCR 全文(*.md)、_data_dump.json、
#    check_report.txt、check_facts.py、__pycache__ 等,
#    都会被默认忽略,不会误传。
#  - 如不想公开 LICENSE,删掉上面的 "!LICENSE" 一行即可。
